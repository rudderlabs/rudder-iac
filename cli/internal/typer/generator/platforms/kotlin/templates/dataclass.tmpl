{{- if .Comment }}
/** {{ .Comment }} */
{{- end }}
@Serializable
data class {{ .Name }}(
{{- range $index, $prop := .Properties }}{{- if $index }},
{{ end }}
{{- if $prop.Comment }}
    /** {{ $prop.Comment }} */
{{- end }}
    @SerialName("{{ $prop.SerialName }}")
    val {{ $prop.Name }}: {{ $prop.Type }}{{ if $prop.Nullable }}?{{ end }}{{ if $prop.Default }} = {{ $prop.Default }}{{ end }}
{{- end }}
){{- if gt (len .NestedClasses) 0 }} {
{{- range $nested :=.NestedClasses }}
{{- include "dataclass.tmpl" $nested | indent $nested.Indent }}
{{- end }}
}
{{- end }}