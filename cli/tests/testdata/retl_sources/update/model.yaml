version: rudder/v1
kind: retl-source-sql-model
metadata:
  name: orders-model
spec:
  id: orders-model
  display_name: Orders Model - revised
  account: "#account:retl-pg"
  primary_key: id
  source_definition: postgres
  enabled: true
  sql: SELECT id, email, created_at FROM orders
