{
  "id": "<ignored>",
  "workspaceId": "<ignored>",
  "version": "<ignored>",
  "externalId": "gcs-template",
  "name": "E2E GCS Template Config",
  "type": "GCS",
  "enabled": true,
  "config": {
    "bucketName": "{{ config.bucketName || rudder-cli-e2e-gcs }}",
    "prefix": "{{ config.prefix || rudder/gcs/ }}",
    "connectionMode": {
      "web": "cloud"
    }
  },
  "createdAt": "<ignored>",
  "updatedAt": "<ignored>"
}
