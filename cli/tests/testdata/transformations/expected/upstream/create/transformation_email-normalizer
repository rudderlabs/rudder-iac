{
  "id": "ignored",
  "versionId": "ignored",
  "workspaceId": "ignored",
  "imports": [],
  "name": "Email Normalizer",
  "description": "Normalizes email addresses in events",
  "language": "javascript",
  "code": "import utilsLib from 'utilsLib';\n\nexport function transformEvent(event, metadata) {\n  if (event.properties && event.properties.email) {\n    event.properties.email = utilsLib.sanitizeEmail(event.properties.email);\n  }\n  return event;\n}\n",
  "externalId": "email-normalizer"
}
