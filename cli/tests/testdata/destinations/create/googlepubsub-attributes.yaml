version: rudder/v1
kind: destination
metadata:
  name: googlepubsub-attributes
spec:
  id: googlepubsub-attributes
  display_name: E2E Google PubSub Attributes
  type: googlepubsub
  enabled: true
  definition_version: 1
  config:
    project_id: rudder-cli-e2e-attrs
    credentials: "{{ .GOOGLE_PUBSUB_CREDENTIALS }}"
    event_to_topic_map:
      - from: Product Viewed
        to: product-events
      - from: Order Completed
        to: order-events
    event_to_attribute_map:
      - from: userId
        to: user_id
