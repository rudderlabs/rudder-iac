version: rudder/v1
kind: destination
metadata:
  name: firebase-minimal
spec:
  id: firebase-minimal
  display_name: E2E Firebase Minimal
  type: firebase
  enabled: true
  definition_version: 1
  config:
    event_filtering:
      blacklist:
        - Application Opened
