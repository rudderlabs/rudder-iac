version: rudder/v1
kind: destination
metadata:
  name: mp-minimal
spec:
  id: mp-minimal
  display_name: E2E Mixpanel Minimal Updated
  type: mp
  enabled: true
  definition_version: 1
  config:
    token: "{{ .MP_TOKEN }}"
    data_residency: eu
    identity_merge_api: original
