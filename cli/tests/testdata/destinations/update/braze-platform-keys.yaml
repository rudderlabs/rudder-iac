version: rudder/v1
kind: destination
metadata:
  name: braze-platform-keys
spec:
  id: braze-platform-keys
  display_name: E2E Braze Platform Keys Updated
  type: braze
  enabled: true
  definition_version: 1
  config:
    data_center: US-05
    rest_api_key: "{{ .BRAZE_REST_API_KEY }}"
    use_platform_specific_api_keys: true
    android_api_key: "{{ .BRAZE_PLATFORM_KEYS_ANDROID_API_KEY_UPDATED }}"
    ios_api_key: "{{ .BRAZE_PLATFORM_KEYS_IOS_API_KEY_UPDATED }}"
    web_api_key: "{{ .BRAZE_PLATFORM_KEYS_WEB_API_KEY_UPDATED }}"
    enable_subscription_group_in_group_call: true
    enable_nested_array_operations: true
    send_purchase_event_with_extra_properties: true
    support_dedup: true
    use_ecommerce_recommended_events: false
    event_filtering:
      blacklist:
        - Page Viewed
    track_anonymous_user:
      web: true
    enable_braze_logging:
      web: true
    enable_push_notification:
      web: true
    allow_user_supplied_javascript:
      web: true
    consent_management:
      web:
        - provider: oneTrust
          consents:
            - analytics
