version: rudder/0.1
kind: transformation
metadata:
  name: test-transformations
spec:
  id: simple-transform
  name: Simple Transform
  description: Adds a greeting message to events
  language: javascript
  code: |
    export function transformEvent(event, metadata) {
      event.properties = event.properties || {};
      event.properties.message = 'Hello World!';
      return event;
    }
