version: rudder/v1
kind: transformation
metadata:
  name: test_transformations
spec:
  id: simple_transform
  name: Simple Transform
  description: Adds source tag and timestamp to events
  language: javascript
  code: |
    export function transformEvent(event, metadata) {
      event.properties = event.properties || {};
      event.properties.source = "rudder";
      event.properties.timestamp = new Date().toISOString();
      return event;
    }
