version: rudder/v1
kind: destination
metadata:
  name: e2e-retl-archive
spec:
  id: e2e-retl-archive
  display_name: E2E RETL Archive
  # Google Ads lists no warehouse source type upstream, so no rETL connection
  # can reach it.
  type: googleads
  enabled: true
  definition_version: 1
  config:
    conversion_id: AW-123456789
