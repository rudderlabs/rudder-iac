version: rudder/v1
kind: destination
metadata:
  name: e2e-validate-http
spec:
  id: e2e-validate-http
  display_name: E2E Validate HTTP
  type: http
  enabled: true
  definition_version: 1
  config:
    api_url: https://webhooks.example.com/rudder/e2e-validate
    auth: noAuth
    method: POST
    format: JSON
    # No connection_mode entry for warehouse sources on purpose.
