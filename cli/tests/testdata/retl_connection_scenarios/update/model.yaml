version: rudder/v1
kind: retl-source-sql-model
metadata:
  name: e2e-scn-model
spec:
  id: e2e-scn-model
  display_name: E2E Scenarios Orders
  account: "#account:e2e-scn-pg"
  primary_key: id
  source_definition: postgres
  enabled: true
  sql: SELECT id, email, plan, updated_at FROM orders
