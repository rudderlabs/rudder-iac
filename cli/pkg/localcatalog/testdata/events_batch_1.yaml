version: rudder/0.1
kind: events
metadata:
  name: app_events
spec:
  events:
    - id: user_signed_up
      name: "User Signed Up"
      event_type: track
      description: "User signed up for the app"
