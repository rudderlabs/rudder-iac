version: rudder/v0.1
kind: properties
metadata:
  name: additional_props
spec:
  properties:
    - id: write_key
      display_name: "Write Key"
      type: string
      description: KSUID identifier for the source embedded in the SDKs
      propConfig:
        minLength: 24
        maxLength: 48

    - id: write_key_1
      display_name: "Write Key 3"
      type: string
      description: KSUID identifier for the source embedded in the SDKs
      propConfig:
        minLength: 24
        maxLength: 48