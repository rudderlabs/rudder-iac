# Feedback

> High-signal human direction, preferences, corrections, or review guidance.
> Append-only. Agent-authored sections may optionally carry an HTML-comment tag
> (e.g., `<!-- pr:<id> -->`) identifying the writer/PR/run; human-authored
> sections are conventionally left untouched by automated runs.

## TEST-1 — Debug Command Output Convention
<!-- ticket:TEST-1 -->
- Debug-style CLI commands should print a yellow `Debug` line before any other output text.
