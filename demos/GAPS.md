# Verification gaps

These steps prove their result in Go, where a viewer cannot see it.
Each one wants a read-only CLI command that shows the same thing on screen.
Where no such command exists, that is a ticket against the CLI.

## TestAccountsApply

- `TestAccountsApply` — Accounts apply
- `TestAccountsApply/apply_create` — Apply create
- `TestAccountsApply/apply_update` — Apply update
- `TestAccountsApply/re-apply_leaves_non-secret_upstream_state_unchanged` — Re-apply leaves non-secret upstream state unchanged

## TestAccountsImportWorkspace

- `TestAccountsImportWorkspace` — Accounts import workspace
