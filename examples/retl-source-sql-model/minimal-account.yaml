version: rudder/v1
kind: account
metadata:
  name: minimal-model-account
spec:
  id: minimal-model-account
  name: Minimal Model Account
  account_definition_name: SOURCE_POSTGRES
  config:
    host: postgres.example.com
    dbname: analytics
    user: rudder
    port: 5432
    sslMode: require
    password: replace-with-postgres-password
