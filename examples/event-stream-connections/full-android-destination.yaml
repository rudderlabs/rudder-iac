version: rudder/v1
kind: destination
metadata:
  name: full-android-destination
spec:
  id: full-android-destination
  display_name: Full Android Destination
  type: webhook
  enabled: true
  definition_version: 1
  config:
    webhook_url: https://webhooks.example.com/android
    connection_mode:
      android: cloud
