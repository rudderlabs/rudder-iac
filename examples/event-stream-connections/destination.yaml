version: rudder/v1
kind: destination
metadata:
  name: event-webhook
spec:
  id: event-webhook
  display_name: Event Webhook
  type: webhook
  enabled: true
  definition_version: 1
  config:
    webhook_url: https://example.com/events
    connection_mode:
      web: cloud
      android_kotlin: cloud
