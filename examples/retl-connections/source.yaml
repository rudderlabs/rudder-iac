version: rudder/v1
kind: retl-source-sql-model
metadata:
  name: customers-model
spec:
  id: customers-model
  display_name: Customers Model
  account: "#account:retl-postgres"
  primary_key: id
  source_definition: postgres
  enabled: true
  sql: SELECT id, email, status FROM customers
