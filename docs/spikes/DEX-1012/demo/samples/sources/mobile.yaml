# yaml-language-server: $schema=../../schemas/event-stream-source.schema.json
version: rudder/v1
kind: event-stream-source
metadata:
  name: mobile-events
spec:
  id: mobile-events
  name: Mobile events
  type: android
  enabled: true
