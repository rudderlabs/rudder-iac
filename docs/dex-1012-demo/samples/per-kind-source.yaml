# yaml-language-server: $schema=../schemas/event-stream-source.json
version: rudder/v1
kind: event-stream-source
metadata:
  name: web-events
spec:
  id: web-events
  name: Web events
  type: web
  enabled: true
